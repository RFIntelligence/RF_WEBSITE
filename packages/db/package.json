{
  "name": "@rf-intelligence/db",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:push:local": "cross-env DATABASE_URL=postgresql://postgres:password@localhost:5433/rf_intelligence_dev prisma db push",
    "db:seed": "ts-node prisma/seed.ts",
    "db:seed:local": "cross-env DATABASE_URL=postgresql://postgres:password@localhost:5433/rf_intelligence_dev ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
