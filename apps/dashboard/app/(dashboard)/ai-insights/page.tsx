import { Sparkles } from "lucide-react";
import { PlaceholderPage } from "@/app/components/placeholder-page";

export default function AIInsightsPage() {
  return (
    <PlaceholderPage
      icon={Sparkles}
      eyebrow="/ ai insights"
      heading="AI Insights"
      body="RF surfaces patterns, risks, and opportunities across your data — so you can focus on the decision, not the analysis."
    />
  );
}
